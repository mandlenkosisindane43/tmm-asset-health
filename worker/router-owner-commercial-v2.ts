import baseCommercial from "./router-owner-commercial";
import { sindaneLogoDataUri } from "./sindane-logo-data";
import { ownerSignatureClearDataUri } from "./owner-signature-clear";

interface ExecutionContext { waitUntil(promise: Promise<unknown>): void; passThroughOnException(): void; }
interface Env { DB: D1Database; BUCKET?: R2Bucket; RESEND_API_KEY?: string; [key:string]: unknown; }
type Row = Record<string, unknown>;
type OwnerSession = { csrf:string; expiresAt:string };

const OWNER_COOKIE="sas_owner_v1";
const OWNER_EMAIL="admin@sindaneassetsolutions.co.za";
const REG_NO="2026/680663/07";
const RESPONSIBLE="M.E Sindane";
const PHONE="0661126886";
const enc=new TextEncoder();

function esc(v:unknown){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]||c));}
function txt(v:unknown,max=800){return String(v??"").trim().slice(0,max);}
function lower(v:unknown){return txt(v,240).toLowerCase();}
function num(v:unknown,f=0){const n=Number(v);return Number.isFinite(n)?n:f;}
function money(v:unknown){return `R ${num(v).toLocaleString("en-ZA",{minimumFractionDigits:2,maximumFractionDigits:2})}`;}
function getCookie(req:Request,name:string){for(const p of (req.headers.get("cookie")||"").split(";")){const i=p.indexOf("=");if(i>-1&&p.slice(0,i).trim()===name)return p.slice(i+1).trim();}return "";}
async function sha256(v:string){return Array.from(new Uint8Array(await crypto.subtle.digest("SHA-256",enc.encode(v))),b=>b.toString(16).padStart(2,"0")).join("");}
async function first(env:Env,sql:string,binds:unknown[]=[]){try{return await env.DB.prepare(sql).bind(...binds).first<Row>();}catch{return null;}}
async function all(env:Env,sql:string,binds:unknown[]=[]){try{return (await env.DB.prepare(sql).bind(...binds).all<Row>()).results||[];}catch{return [];}}
function redirect(location:string){return new Response(null,{status:303,headers:{location,"cache-control":"no-store"}});}
function safeFileName(v:string){return (v||"bank-statement").replace(/[^a-zA-Z0-9._-]+/g,"-").slice(0,120)||"bank-statement";}
async function ownerSession(req:Request,env:Env):Promise<OwnerSession|null>{const token=getCookie(req,OWNER_COOKIE);if(!token)return null;const r=await first(env,"SELECT csrf_token AS csrf,expires_at AS expiresAt FROM owner_sessions_v1 WHERE token_hash=? LIMIT 1",[await sha256(token)]);if(!r||new Date(String(r.expiresAt)).getTime()<Date.now())return null;return{csrf:String(r.csrf||""),expiresAt:String(r.expiresAt||"")};}

let schemaPromise:Promise<void>|null=null;
async function ensureSchema(env:Env){if(schemaPromise)return schemaPromise;schemaPromise=(async()=>{
  await env.DB.prepare(`CREATE TABLE IF NOT EXISTS owner_quotations_v2 (id INTEGER PRIMARY KEY AUTOINCREMENT,company_id INTEGER NOT NULL,quote_number TEXT NOT NULL UNIQUE,recipient_email TEXT NOT NULL,description TEXT NOT NULL,licence_days INTEGER NOT NULL DEFAULT 180,subtotal REAL NOT NULL DEFAULT 0,vat_percent REAL NOT NULL DEFAULT 15,notes TEXT,status TEXT NOT NULL DEFAULT 'draft',public_token TEXT NOT NULL UNIQUE,created_at TEXT NOT NULL,sent_at TEXT)`).run();
  const alters=[
    "ALTER TABLE owner_quotations_v2 ADD COLUMN owner_tax_number TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN owner_address TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN bank_name TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN bank_account_name TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN bank_account_number TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN bank_account_type TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN bank_branch_code TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN bank_statement_key TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN bank_statement_file TEXT",
    "ALTER TABLE owner_quotations_v2 ADD COLUMN bank_details_verified INTEGER NOT NULL DEFAULT 0"
  ];
  for(const s of alters){try{await env.DB.prepare(s).run();}catch{} }
  await env.DB.prepare(`CREATE TABLE IF NOT EXISTS owner_commercial_profile_v3 (id INTEGER PRIMARY KEY CHECK(id=1),tax_number TEXT,residential_address TEXT,updated_at TEXT NOT NULL)`).run();
})().catch(e=>{schemaPromise=null;throw e;});return schemaPromise;}

const css=`<style>*{box-sizing:border-box}body{margin:0;background:#f5f7f9;color:#122033;font-family:Arial,Helvetica,sans-serif}.top{background:#061827;color:#fff;padding:15px 22px;display:flex;justify-content:space-between;align-items:center}.top b{font-size:18px}.top small{display:block;color:#b9c9d4;margin-top:3px}.wrap{max-width:1180px;margin:auto;padding:20px}.panel{background:#fff;border:1px solid #dfe5ea;border-radius:11px;padding:16px;margin-top:12px}.panel h1{margin:0;font-size:27px}.panel h2{font-size:15px;margin:0 0 12px}.muted{font-size:11px;color:#687786;line-height:1.5}.field{display:grid;gap:5px;font-size:11px;font-weight:800;margin:9px 0}.field input,.field select,.field textarea{padding:10px;border:1px solid #ccd6df;border-radius:7px;font:inherit;width:100%}.field textarea{min-height:90px}.grid2{display:grid;grid-template-columns:1fr 1fr;gap:10px}.grid3{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.btn{display:inline-block;border:0;border-radius:7px;padding:10px 13px;background:#11975c;color:#fff;text-decoration:none;font-size:11px;font-weight:900;cursor:pointer}.btn.blue{background:#1768d5}.btn.amber{background:#d98a00}.btn.gray{background:#eef2f6;color:#24364b;border:1px solid #d6dee6}.actions{display:flex;gap:7px;flex-wrap:wrap}.notice{padding:11px;border-radius:8px;background:#eaf7ef;color:#166534;font-size:11px;margin:10px 0}.warn{background:#fff7e8;color:#76540b;border:1px solid #efd79d}.docbar{max-width:900px;margin:18px auto 0;display:flex;gap:8px;flex-wrap:wrap}.doc{max-width:900px;margin:18px auto;background:#fff;border:1px solid #dfe5ea;padding:40px 46px;box-shadow:0 8px 30px rgba(15,23,42,.07)}.dochead{display:flex;justify-content:space-between;gap:20px;border-bottom:3px solid #11975c;padding-bottom:15px}.dochead img{width:220px;height:110px;object-fit:contain}.meta{text-align:right;font-size:11px;line-height:1.6}.title{font-size:28px;margin:23px 0 8px}.info{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin:16px 0}.box{border:1px solid #dfe5ea;border-radius:9px;padding:12px;font-size:11px;line-height:1.65}.doc table{width:100%;border-collapse:collapse;margin:18px 0;font-size:12px}.doc th,.doc td{border:1px solid #dfe5ea;padding:10px;text-align:left}.doc th{background:#f5f7f9}.totals{margin-left:auto;width:390px}.total-payable th,.total-payable td{background:#eaf7ef;color:#075d39;font-size:14px;font-weight:900}.tax-note{font-size:10px;color:#76540b;background:#fff7e8;border:1px solid #efd79d;border-radius:7px;padding:8px;margin-top:8px}.sign{margin-top:32px}.sign img{display:block;width:520px;max-width:90%;height:95px;object-fit:contain;object-position:left center}.signline{width:520px;max-width:90%;border-top:1px solid #4a5560}.footer{margin-top:28px;border-top:1px solid #dfe5ea;padding-top:10px;font-size:9px;color:#667583}.bankgrid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.badge{display:inline-block;padding:4px 7px;border-radius:999px;background:#e7f7ed;color:#087044;font-size:9px;font-weight:900}@media(max-width:760px){.grid2,.grid3,.info,.bankgrid{grid-template-columns:1fr}.doc{padding:22px;margin:0}.dochead{display:block}.meta{text-align:left}.wrap{padding:12px}}@media print{.top,.no-print,.docbar{display:none!important}body{background:#fff}.doc{box-shadow:none;border:0;max-width:none;margin:0;padding:0}}</style>`;
function htmlPage(title:string,body:string){return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title>${css}</head><body><header class="top no-print"><div><b>Sindane Asset Solutions · Platform Owner</b><small>TMM Asset Health Commercial Documents</small></div><a class="btn gray" href="/owner-commercial">Commercial Home</a></header>${body}</body></html>`,{headers:{"content-type":"text/html; charset=utf-8","cache-control":"private, no-store","x-frame-options":"DENY","content-security-policy":"default-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; script-src 'self' 'unsafe-inline'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"}});}
function docHeader(ref:string,date:string){return `<div class="dochead"><img src="${sindaneLogoDataUri()}" alt="Sindane Asset Solutions"><div class="meta"><b>SINDANE ASSET SOLUTIONS (PTY) LTD</b><br>Registration No: ${REG_NO}<br>Email: ${OWNER_EMAIL}<br>Cell / WhatsApp: ${PHONE}<br><b>QUOTATION</b><br>Reference: ${esc(ref)}<br>Date: ${esc(date)}</div></div>`;}
function signatureBlock(){return `<div class="sign"><b>Issued / Approved by</b><img src="${ownerSignatureClearDataUri()}" alt="M.E Sindane signature"><div class="signline"></div><div style="margin-top:8px"><b>${RESPONSIBLE}</b><br>Responsible Person · Sindane Asset Solutions<br>Cell / WhatsApp: ${PHONE}</div></div>`;}
function bankBlock(q:Row){const has=String(q.bankName||q.bankAccountNumber||q.bankAccountName||"").trim();if(!has)return `<div class="box"><b>Bank Details</b><br><span class="muted">Bank details will be confirmed separately by Sindane Asset Solutions.</span></div>`;return `<div class="box"><b>Bank Details</b><br>Bank: <b>${esc(q.bankName||"—")}</b><br>Account holder: ${esc(q.bankAccountName||"—")}<br>Account number: <b>${esc(q.bankAccountNumber||"—")}</b><br>Account type: ${esc(q.bankAccountType||"—")}<br>Branch code: ${esc(q.bankBranchCode||"—")}</div>`;}
function quoteBody(q:Row,c:Row){const quoted=num(q.subtotal),rate=Math.max(0,num(q.vatPercent)),tax=quoted*rate/100,created=String(q.createdAt||new Date().toISOString()).slice(0,10);return `${docHeader(String(q.quoteNumber),created)}<h1 class="title">Quotation</h1><div class="info"><div class="box"><b>Prepared for</b><br>${esc(c.name)}<br>Company Admin Email: <b>${esc(q.recipientEmail)}</b></div><div class="box"><b>Sindane Commercial Details</b><br>Registration No: ${REG_NO}<br>Income Tax No: <b>${esc(q.ownerTaxNumber||"Not set")}</b><br>Residential / Business Address: ${esc(q.ownerAddress||"Not set")}<br>Cell / WhatsApp: ${PHONE}<br>Email: ${OWNER_EMAIL}</div></div><table><thead><tr><th>Description</th><th>Licence period</th><th>Quoted amount</th></tr></thead><tbody><tr><td>${esc(q.description)}</td><td>${num(q.licenceDays)} days</td><td><b>${money(quoted)}</b></td></tr></tbody></table><table class="totals"><tr><th>Quoted amount</th><td>${money(quoted)}</td></tr><tr><th>Tax reference (${rate.toFixed(1)}%)</th><td>${money(tax)}</td></tr><tr class="total-payable"><th>TOTAL PAYABLE</th><td>${money(quoted)}</td></tr></table><div class="tax-note"><b>Tax display rule:</b> the tax amount is shown for reference only and is <b>not added</b> to the quoted total. The total payable remains ${money(quoted)}.</div><div class="info">${bankBlock(q)}<div class="box"><b>Contact</b><br>${RESPONSIBLE}<br>${OWNER_EMAIL}<br>${PHONE}</div></div>${q.notes?`<div class="box"><b>Notes / Terms</b><br>${esc(q.notes).replace(/\n/g,"<br>")}</div>`:""}${signatureBlock()}<div class="footer">Sindane Asset Solutions (Pty) Ltd · Registration ${REG_NO} · ${OWNER_EMAIL} · ${PHONE} · TRACK. PREVENT. PERFORM.</div>`;}
function wordResponse(filename:string,body:string){return new Response(`<!doctype html><html><head><meta charset="utf-8"><title>Quotation</title></head><body>${body}</body></html>`,{headers:{"content-type":"application/msword; charset=utf-8","content-disposition":`attachment; filename="${safeFileName(filename)}"`,"cache-control":"private, no-store"}});}

async function profile(env:Env){return await first(env,"SELECT tax_number AS taxNumber,residential_address AS residentialAddress FROM owner_commercial_profile_v3 WHERE id=1")||{};}
async function companyAdmin(env:Env,cid:number){return await first(env,"SELECT full_name AS fullName,email FROM contractor_accounts WHERE company_id=? AND role IN ('company_admin','admin') ORDER BY id LIMIT 1",[cid])||{};}
async function quote(env:Env,id:number){return await first(env,`SELECT q.id,q.company_id AS companyId,q.quote_number AS quoteNumber,q.recipient_email AS recipientEmail,q.description,q.licence_days AS licenceDays,q.subtotal,q.vat_percent AS vatPercent,q.notes,q.status,q.public_token AS publicToken,q.created_at AS createdAt,q.sent_at AS sentAt,q.owner_tax_number AS ownerTaxNumber,q.owner_address AS ownerAddress,q.bank_name AS bankName,q.bank_account_name AS bankAccountName,q.bank_account_number AS bankAccountNumber,q.bank_account_type AS bankAccountType,q.bank_branch_code AS bankBranchCode,q.bank_statement_key AS bankStatementKey,q.bank_statement_file AS bankStatementFile,q.bank_details_verified AS bankDetailsVerified,c.name FROM owner_quotations_v2 q JOIN companies c ON c.id=q.company_id WHERE q.id=?`,[id]);}
async function quoteByToken(env:Env,tok:string){return await first(env,`SELECT q.id,q.company_id AS companyId,q.quote_number AS quoteNumber,q.recipient_email AS recipientEmail,q.description,q.licence_days AS licenceDays,q.subtotal,q.vat_percent AS vatPercent,q.notes,q.status,q.public_token AS publicToken,q.created_at AS createdAt,q.sent_at AS sentAt,q.owner_tax_number AS ownerTaxNumber,q.owner_address AS ownerAddress,q.bank_name AS bankName,q.bank_account_name AS bankAccountName,q.bank_account_number AS bankAccountNumber,q.bank_account_type AS bankAccountType,q.bank_branch_code AS bankBranchCode,q.bank_statement_key AS bankStatementKey,q.bank_statement_file AS bankStatementFile,q.bank_details_verified AS bankDetailsVerified,c.name FROM owner_quotations_v2 q JOIN companies c ON c.id=q.company_id WHERE q.public_token=?`,[tok]);}
async function nextQuoteNo(env:Env){const y=new Date().getFullYear(),r=await first(env,"SELECT COALESCE(MAX(id),0)+1 n FROM owner_quotations_v2");return `SAS-Q-${y}-${String(num(r?.n,1)).padStart(4,"0")}`;}
function makeToken(){const b=crypto.getRandomValues(new Uint8Array(24));let s="";b.forEach(x=>s+=String.fromCharCode(x));return btoa(s).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/g,"");}

async function newQuotePage(env:Env,s:OwnerSession){const cs=await all(env,`SELECT c.id,c.name,(SELECT email FROM contractor_accounts a WHERE a.company_id=c.id AND a.role IN ('company_admin','admin') ORDER BY a.id LIMIT 1) adminEmail FROM companies c WHERE lower(c.licence_status)!='banned' ORDER BY c.name`),p=await profile(env);return htmlPage("Create Quotation",`<main class="wrap"><section class="panel"><h1>Create Contractor Quotation</h1><p class="muted">The contractor Company Admin email is used automatically unless you override it. Your tax number and address are remembered for your next quotation.</p><form method="post" action="/owner-commercial/action"><input type="hidden" name="csrf" value="${esc(s.csrf)}"><input type="hidden" name="action" value="create-quote-v2"><label class="field">Contractor company<select name="companyId" required><option value="">Choose contractor</option>${cs.map(c=>`<option value="${num(c.id)}">${esc(c.name)}${c.adminEmail?` — ${esc(c.adminEmail)}`:""}</option>`).join("")}</select></label><label class="field">Company Admin email override (optional)<input type="email" name="recipientEmail" placeholder="Leave blank to use the contractor administrator email"></label><div class="grid2"><label class="field">Sindane Income Tax Number<input name="taxNumber" value="${esc(p.taxNumber||"")}" placeholder="Enter your income tax number"></label><label class="field">Residential / Business Address<input name="ownerAddress" value="${esc(p.residentialAddress||"")}" placeholder="Enter the address to appear on quotations"></label></div><label class="field">Description<textarea name="description" required>TMM Asset Health software licence and support</textarea></label><div class="grid3"><label class="field">Licence days<input name="licenceDays" type="number" min="1" max="3650" value="180" required></label><label class="field">TOTAL quoted amount (ZAR)<input name="subtotal" type="number" min="0" step="0.01" placeholder="Example: 9000" required></label><label class="field">Tax reference % (NOT added to total)<input name="vat" type="number" min="0" max="100" step="0.01" value="15"></label></div><div class="notice warn"><b>Example:</b> if you enter R9,000, the quotation will show the tax reference separately but <b>Total Payable remains R9,000</b>.</div><label class="field">Notes / Terms<textarea name="notes" placeholder="Payment terms, scope, conditions..."></textarea></label><div class="actions"><button class="btn" type="submit">Create Quotation</button><button class="btn blue" name="sendNow" value="1" type="submit">Create & Send Email</button><a class="btn gray" href="/owner-commercial">Cancel</a></div></form></section></main>`);}

function extractPrintable(bytes:Uint8Array){let out="";for(const b of bytes){if(b===10||b===13||b===9||(b>=32&&b<=126))out+=String.fromCharCode(b);else out+=" ";}return out.replace(/\s+/g," ");}
function cleanDigits(v:string){return v.replace(/[^0-9]/g,"");}
function extractBankDetails(text:string){const t=text.replace(/\s+/g," ");let bankName="";const banks:[[RegExp,string]]|Array<[RegExp,string]>=[[/first national bank|\bfnb\b/i,"FNB / First National Bank"],[/standard bank/i,"Standard Bank"],[/\babsa\b/i,"ABSA"],[/nedbank/i,"Nedbank"],[/capitec/i,"Capitec Bank"],[/tymebank|tyme bank/i,"TymeBank"],[/investec/i,"Investec"],[/african bank/i,"African Bank"],[/discovery bank/i,"Discovery Bank"]];for(const [re,n] of banks){if(re.test(t)){bankName=n;break;}}
  const accountNumber=(t.match(/(?:account\s*(?:number|no\.?|#)|acc\s*no\.?)\s*[:\-]?\s*([0-9][0-9 \-]{5,22})/i)||[])[1]||"";
  const branch=(t.match(/(?:branch\s*(?:code|number)|universal\s*branch\s*code)\s*[:\-]?\s*([0-9][0-9 \-]{3,10})/i)||[])[1]||"";
  const type=(t.match(/(?:account\s*type|type\s*of\s*account)\s*[:\-]?\s*([A-Za-z ]{3,30})/i)||[])[1]||"";
  const holder=(t.match(/(?:account\s*(?:holder|name)|name\s*of\s*account)\s*[:\-]?\s*([A-Za-z0-9 .&()'\-]{3,80})/i)||[])[1]||"";
  return{bankName,bankAccountName:txt(holder,100),bankAccountNumber:cleanDigits(accountNumber).slice(0,24),bankAccountType:txt(type,60),bankBranchCode:cleanDigits(branch).slice(0,12)};
}

async function uploadBankStatement(req:Request,env:Env,s:OwnerSession,form:FormData){const id=num(form.get("quoteId"));const q=await quote(env,id);if(!q)return redirect("/owner-commercial?msg=Quotation+not+found");const file=form.get("bankStatement");if(!(file instanceof File)||file.size===0)return redirect(`/owner-commercial/quotation?id=${id}&msg=Choose+a+bank+statement+first`);if(file.size>10*1024*1024)return redirect(`/owner-commercial/quotation?id=${id}&msg=Bank+statement+must+be+10MB+or+smaller`);const bytes=new Uint8Array(await file.arrayBuffer());let content="";if(file.type.startsWith("text/")||/\.(txt|csv)$/i.test(file.name)){content=new TextDecoder().decode(bytes);}else{content=extractPrintable(bytes);}const details=extractBankDetails(content);const key=`owner-commercial/bank-statements/quote-${id}/${Date.now()}-${safeFileName(file.name)}`;if(env.BUCKET)await env.BUCKET.put(key,bytes,{httpMetadata:{contentType:file.type||"application/octet-stream"},customMetadata:{quoteId:String(id),uploadedBy:"platform-owner"}});await env.DB.prepare(`UPDATE owner_quotations_v2 SET bank_name=?,bank_account_name=?,bank_account_number=?,bank_account_type=?,bank_branch_code=?,bank_statement_key=?,bank_statement_file=?,bank_details_verified=0 WHERE id=?`).bind(details.bankName||null,details.bankAccountName||null,details.bankAccountNumber||null,details.bankAccountType||null,details.bankBranchCode||null,key,file.name,id).run();return redirect(`/owner-commercial/quotation?id=${id}&msg=Bank+statement+opened.+Confirm+the+bank+details+below`);}

async function quoteView(env:Env,s:OwnerSession,id:number,url:URL){const q=await quote(env,id);if(!q)return new Response("Quotation not found",{status:404});const msg=url.searchParams.get("msg");const body=quoteBody(q,{name:q.name});return htmlPage(`Quotation ${q.quoteNumber}`,`<div class="docbar no-print"><a class="btn gray" href="/owner-commercial">← Back</a><button class="btn" onclick="window.print()">Print</button><button class="btn blue" onclick="window.print()">Save PDF</button><a class="btn gray" href="/owner-commercial/quotation/${id}.doc">Save Word</a><form method="post" action="/owner-commercial/action"><input type="hidden" name="csrf" value="${esc(s.csrf)}"><input type="hidden" name="action" value="send-quote-v2"><input type="hidden" name="quoteId" value="${id}"><button class="btn amber" type="submit">Send Quotation Email</button></form></div><main class="wrap no-print">${msg?`<div class="notice">${esc(msg)}</div>`:""}<section class="panel"><h2>Bank Statement for this Quotation</h2><p class="muted">Upload the bank statement you want to use for this contractor. Click <b>Open Bank Statement</b>; the system will attempt to read the bank details and then you can confirm/correct them.</p><form method="post" action="/owner-commercial/action" enctype="multipart/form-data"><input type="hidden" name="csrf" value="${esc(s.csrf)}"><input type="hidden" name="action" value="upload-bank-statement"><input type="hidden" name="quoteId" value="${id}"><label class="field">Insert bank statement (PDF / image / TXT / CSV)<input type="file" name="bankStatement" accept=".pdf,.png,.jpg,.jpeg,.txt,.csv,application/pdf,image/*,text/*" required></label><button class="btn blue" type="submit">Open Bank Statement</button>${q.bankStatementFile?` <a class="btn gray" href="/owner-commercial/bank-statement?quoteId=${id}">View uploaded statement: ${esc(q.bankStatementFile)}</a>`:""}</form></section><section class="panel"><h2>Confirm Bank Details</h2><form method="post" action="/owner-commercial/action"><input type="hidden" name="csrf" value="${esc(s.csrf)}"><input type="hidden" name="action" value="save-bank-details"><input type="hidden" name="quoteId" value="${id}"><div class="bankgrid"><label class="field">Bank name<input name="bankName" value="${esc(q.bankName||"")}"></label><label class="field">Account holder<input name="bankAccountName" value="${esc(q.bankAccountName||"")}"></label><label class="field">Account number<input name="bankAccountNumber" value="${esc(q.bankAccountNumber||"")}"></label><label class="field">Account type<input name="bankAccountType" value="${esc(q.bankAccountType||"")}"></label><label class="field">Branch code<input name="bankBranchCode" value="${esc(q.bankBranchCode||"")}"></label><div class="field"><span>Verification</span><span class="badge">${num(q.bankDetailsVerified)?"Confirmed":"Please confirm"}</span></div></div><button class="btn" type="submit">Confirm Bank Details</button></form><p class="muted">Automatic reading is best-effort because banks use different PDF/image formats. Always confirm the details before sending the quotation.</p></section></main><article class="doc">${body}</article>`);}

async function sendQuote(env:Env,id:number,origin:string){const q=await quote(env,id);if(!q)throw new Error("Quotation not found");if(!env.RESEND_API_KEY)throw new Error("RESEND_API_KEY is not configured");const total=num(q.subtotal),link=`${origin}/quotation/${encodeURIComponent(String(q.publicToken))}`;const res=await fetch("https://api.resend.com/emails",{method:"POST",headers:{authorization:`Bearer ${env.RESEND_API_KEY}`,"content-type":"application/json"},body:JSON.stringify({from:"Sindane Asset Solutions <admin@sindaneassetsolutions.co.za>",to:[String(q.recipientEmail)],subject:`Quotation ${q.quoteNumber} · TMM Asset Health`,html:`<div style="font-family:Arial,sans-serif;max-width:680px;margin:auto"><h2>Sindane Asset Solutions</h2><p>Dear Company Administrator,</p><p>Please find quotation <b>${esc(q.quoteNumber)}</b> for <b>${esc(q.name)}</b>.</p><p><b>Total payable: ${money(total)}</b></p><p><a href="${link}" style="display:inline-block;background:#11975c;color:white;padding:12px 18px;border-radius:7px;text-decoration:none;font-weight:bold">Open Quotation</a></p><p>The secure quotation contains the confirmed commercial and bank details, company registration information and authorised signature.</p><p>Regards,<br><b>${RESPONSIBLE}</b><br>Sindane Asset Solutions<br>${PHONE}</p></div>`})});if(!res.ok){const j=await res.json().catch(()=>({}));throw new Error(String((j as Row).message||`Email provider HTTP ${res.status}`));}await env.DB.prepare("UPDATE owner_quotations_v2 SET status='sent',sent_at=? WHERE id=?").bind(new Date().toISOString(),id).run();}

async function handleAction(req:Request,env:Env,s:OwnerSession){const clone=req.clone();const form=await req.formData();if(String(form.get("csrf")||"")!==s.csrf)return new Response("Invalid request token",{status:403});const action=lower(form.get("action"));if(action==="create-quote-v2"){const cid=num(form.get("companyId")),c=await first(env,"SELECT id,name FROM companies WHERE id=?",[cid]);if(!c)return redirect("/owner-commercial?msg=Company+not+found");const admin=await companyAdmin(env,cid),recipient=lower(form.get("recipientEmail"))||lower(admin.email);if(!recipient.includes("@"))return redirect("/owner-commercial/quotation/new?msg=Contractor+administrator+email+is+missing");const tax=txt(form.get("taxNumber"),80),address=txt(form.get("ownerAddress"),500),now=new Date().toISOString();await env.DB.prepare(`INSERT INTO owner_commercial_profile_v3(id,tax_number,residential_address,updated_at) VALUES(1,?,?,?) ON CONFLICT(id) DO UPDATE SET tax_number=excluded.tax_number,residential_address=excluded.residential_address,updated_at=excluded.updated_at`).bind(tax||null,address||null,now).run();const qno=await nextQuoteNo(env),desc=txt(form.get("description"),1500),days=Math.max(1,Math.min(3650,num(form.get("licenceDays"),180))),quoted=Math.max(0,num(form.get("subtotal"))),rate=Math.max(0,Math.min(100,num(form.get("vat"),15))),notes=txt(form.get("notes"),2500),pub=makeToken();await env.DB.prepare(`INSERT INTO owner_quotations_v2(company_id,quote_number,recipient_email,description,licence_days,subtotal,vat_percent,notes,status,public_token,created_at,owner_tax_number,owner_address) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(cid,qno,recipient,desc,days,quoted,rate,notes||null,"draft",pub,now,tax||null,address||null).run();const r=await first(env,"SELECT id FROM owner_quotations_v2 WHERE quote_number=?",[qno]),id=num(r?.id);if(String(form.get("sendNow")||"")==="1"){try{await sendQuote(env,id,new URL(req.url).origin);}catch(e){return redirect(`/owner-commercial/quotation?id=${id}&msg=${encodeURIComponent(e instanceof Error?e.message:String(e))}`);}}return redirect(`/owner-commercial/quotation?id=${id}`);}if(action==="send-quote-v2"){const id=num(form.get("quoteId"));try{await sendQuote(env,id,new URL(req.url).origin);return redirect(`/owner-commercial/quotation?id=${id}&msg=Quotation+sent+successfully`);}catch(e){return redirect(`/owner-commercial/quotation?id=${id}&msg=${encodeURIComponent(e instanceof Error?e.message:String(e))}`);}}if(action==="upload-bank-statement")return uploadBankStatement(req,env,s,form);if(action==="save-bank-details"){const id=num(form.get("quoteId"));await env.DB.prepare(`UPDATE owner_quotations_v2 SET bank_name=?,bank_account_name=?,bank_account_number=?,bank_account_type=?,bank_branch_code=?,bank_details_verified=1 WHERE id=?`).bind(txt(form.get("bankName"),100)||null,txt(form.get("bankAccountName"),100)||null,txt(form.get("bankAccountNumber"),40)||null,txt(form.get("bankAccountType"),60)||null,txt(form.get("bankBranchCode"),30)||null,id).run();return redirect(`/owner-commercial/quotation?id=${id}&msg=Bank+details+confirmed`);}return baseCommercial.fetch(clone,env as never,{waitUntil(){},passThroughOnException(){}} as ExecutionContext);}

async function publicQuote(env:Env,tok:string,word=false){const q=await quoteByToken(env,tok);if(!q)return new Response("Quotation not found or link invalid",{status:404});const body=quoteBody(q,{name:q.name});if(word)return wordResponse(`${q.quoteNumber}.doc`,body);return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(q.quoteNumber)}</title>${css}</head><body><div class="docbar no-print"><button class="btn" onclick="window.print()">Print</button><button class="btn blue" onclick="window.print()">Save PDF</button><a class="btn gray" href="/quotation/${encodeURIComponent(tok)}/word">Save Word</a></div><article class="doc">${body}</article></body></html>`,{headers:{"content-type":"text/html; charset=utf-8","cache-control":"private, no-store","content-security-policy":"default-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; script-src 'self' 'unsafe-inline'; frame-ancestors 'none'; base-uri 'self'"}});}

export default{async fetch(req:Request,env:Env,ctx:ExecutionContext):Promise<Response>{await ensureSchema(env);const url=new URL(req.url),p=url.pathname;if(p.startsWith("/quotation/")){const parts=p.slice(11).split("/"),tok=decodeURIComponent(parts[0]||"");return publicQuote(env,tok,parts[1]==="word");}if(p.startsWith("/owner-commercial")){const s=await ownerSession(req,env);if(!s)return redirect("/owner-login");if(p==="/owner-commercial/quotation/new"&&req.method==="GET")return newQuotePage(env,s);if(p==="/owner-commercial/quotation"&&req.method==="GET")return quoteView(env,s,num(url.searchParams.get("id")),url);const dm=p.match(/^\/owner-commercial\/quotation\/(\d+)\.doc$/);if(dm){const q=await quote(env,Number(dm[1]));if(!q)return new Response("Quotation not found",{status:404});return wordResponse(`${q.quoteNumber}.doc`,quoteBody(q,{name:q.name}));}if(p==="/owner-commercial/bank-statement"&&req.method==="GET"){const q=await quote(env,num(url.searchParams.get("quoteId")));if(!q||!q.bankStatementKey||!env.BUCKET)return new Response("Bank statement not found",{status:404});const obj=await env.BUCKET.get(String(q.bankStatementKey));if(!obj)return new Response("Bank statement not found",{status:404});const h=new Headers();obj.writeHttpMetadata(h);h.set("content-disposition",`inline; filename="${safeFileName(String(q.bankStatementFile||"bank-statement"))}"`);h.set("cache-control","private, no-store");return new Response(obj.body,{headers:h});}if(p==="/owner-commercial/action"&&req.method==="POST")return handleAction(req,env,s);}return baseCommercial.fetch(req,env as never,ctx);}};
